---
apiVersion: v1
kind: Namespace
metadata:
  name: qcguy
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-secrets
  namespace: qcguy
---
#kind: ConfigMap
#apiVersion: v1
#metadata:
#  name: qcguy-configmap
#  namespace: qcguy
#data:
  # Configuration values can be set as key-value properties
  #database: mongodb
  #database_uri: mongodb://localhost:27017
  # Or set as complete file contents (even JSON!)
#  fromfile: /home/vik/IdeaProjects/qcguy-cms/config/
---
apiVersion: v1
kind: Service
metadata:
  name: qcguy
  namespace: qcguy
  labels:
    app: qcguy
    io.kompose.service: qcguy
spec:
  type: NodePort
  #loadBalancerIP: 192.168.49.234
  selector:
    io.kompose.service: qcguy
    app: qcguy
  ports:
    - port: 2368
      protocol: TCP
      name: "2368"
      nodePort: 30368
      targetPort: 2368
  #externalTrafficPolicy: Local
#status:
  #loadBalancer:
  #  ingress:
  #    - ip: 192.168.49.234
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: qcguy-pv-volume
  namespace: qcguy
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 300Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/qcguy-ghost
#  storageClassName: do-block-storage
#apiVersion: v1
#kind: PersistentVolume
#metadata:
#  name: qcguy-content
#  labels:
#    app: qcguy-content
#spec:
#  storageClassName: ""
#  capacity:
#    storage: 250Mi
#  accessModes:
#    - ReadWriteOnce
#  persistentVolumeReclaimPolicy: Retain
#  claimRef:
#    namespace: qcguy
#    name: qcguy-content
#  nfs:
#    path: /tmp
#    server: 172.17.0.2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  name: qcguy-content-claim
  namespace: qcguy
  labels:
    app: qcguy-content-claim
    io.kompose.service: qcguy-content-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi
  storageClassName: manual
status: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    app: qcguy
    io.kompose.service: qcguy
  name: qcguy
  namespace: qcguy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qcguy
      io.kompose.service: qcguy
  # Keep the old pod serving until the new one is Ready, so Cloudflare never
  # hits an empty NodePort during a rollout (caused visitor-facing 502s).
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.26.0 (40646f47)
        # --- Vault Agent Injector: render config.production.json from kv/qcguy/ghost ---
        # Secret is owned in this repo (vault/ghost.secret.sops.env) and refreshed at
        # deploy by the vaultSync(app:'qcguy') stage in the Jenkinsfile.
        vault.hashicorp.com/agent-inject: 'true'
        vault.hashicorp.com/role: 'qcguy-role'
        vault.hashicorp.com/agent-pre-populate-only: 'true'
        vault.hashicorp.com/agent-inject-secret-config.production.json: 'kv/qcguy/ghost'
        vault.hashicorp.com/agent-inject-template-config.production.json: |
          {{- with secret "kv/qcguy/ghost" -}}{{ .Data.data.CONFIG_PRODUCTION_JSON_B64 | base64Decode }}{{- end -}}
      creationTimestamp: null
      labels:
        io.kompose.service: qcguy
        app: qcguy
    spec:
      serviceAccountName: vault-secrets
      volumes:
        - name: qcguy-content-storage
          persistentVolumeClaim:
            claimName: qcguy-content-claim
      containers:
        - env:
            - name: url
              value: https://www.qcguy.com
          image: ghost:latest
          #imagePullPolicy: Always
          name: qcguy
          # Copy the Vault-rendered config to where Ghost expects it, then start Ghost
          # (replicates the image's default ENTRYPOINT+CMD: docker-entrypoint.sh node current/index.js).
          command: ["/bin/sh","-c"]
          args:
            - cp /vault/secrets/config.production.json /var/lib/ghost/config.production.json && exec docker-entrypoint.sh node current/index.js
          ports:
            - containerPort: 2368
          # Gate traffic on Ghost actually serving HTTP. A 200/301 from / marks the
          # pod Ready; combined with maxUnavailable:0 the rollout is seamless.
          # Generous failureThreshold covers Ghost's ~20-40s boot without a startupProbe.
          readinessProbe:
            httpGet:
              path: /
              port: 2368
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          # Ghost CMS, ~100 visitors/day. Observed ~595Mi steady, ~1.25 CPU on spikes.
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "2"
              memory: "1Gi"
          volumeMounts:
            - mountPath: /var/lib/ghost/content #/mnt/qcguy-ghost/data
              name: qcguy-content-storage
      #restartPolicy: Never
status: {}